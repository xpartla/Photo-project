---
import BaseLayout from "../../../../../layouts/BaseLayout.astro";
import AdminBookingList from "../../../../../components/AdminBookingList.astro";
import { t } from "../../../../../lib/i18n";

const lang = "en";
const pageTitle = `${t(lang, "admin.booking.allBookings")} — PartlPhoto`;
---

<BaseLayout title={pageTitle} lang={lang}>
  <main class="admin">
    <AdminBookingList lang={lang} />
  </main>
</BaseLayout>

<style lang="scss" is:global>
  @use "../../../../../styles/components/admin";
  @use "../../../../../styles/components/account";
</style>

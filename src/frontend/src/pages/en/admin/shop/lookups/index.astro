---
import BaseLayout from "../../../../../layouts/BaseLayout.astro";
import AdminLookupManager from "../../../../../components/AdminLookupManager.astro";
import { t } from "../../../../../lib/i18n";

const lang = "en";
const pageTitle = `${t(lang, "admin.shop.lookups")} — PartlPhoto`;
---

<BaseLayout title={pageTitle} lang={lang}>
  <main class="admin">
    <AdminLookupManager lang={lang} />
  </main>
</BaseLayout>

<style lang="scss" is:global>
  @use "../../../../../styles/components/admin";
  @use "../../../../../styles/components/account";
</style>
